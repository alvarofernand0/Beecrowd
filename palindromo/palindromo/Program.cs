// Teste para verificar se uma string é palíndromo
using System;

class Program
{
    static void Main()
    {
        System.Console.WriteLine("Digite uma palavra ou frase: ");
        string entrada = Console.ReadLine();
        string semEspaco = entrada.Replace(" ", "").ToLower();
        string reverso = "";

        if (!entrada.Contains(string.Empty))
        {
            System.Console.WriteLine("Digite apenas letras!");
        }
        
        for (int i = semEspaco.)
    }
}